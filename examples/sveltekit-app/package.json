{
  "name": "sveltekit-app-example",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "pnpm exec turbo run build --filter=@pinagent/vite-plugin",
    "dev": "vite dev",
    "predev:dogfood": "pnpm exec turbo run build --filter=@pinagent/vite-plugin --filter=@pinagent/mcp",
    "dev:dogfood": "PINAGENT_SPAWN_AGENT=off vite dev",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@pinagent/vite-plugin": "workspace:*",
    "@sveltejs/adapter-node": "^5.0.0",
    "@sveltejs/kit": "^2.0.0",
    "@sveltejs/vite-plugin-svelte": "^7.0.0",
    "svelte": "^5.56.1",
    "typescript": "^6.0.3",
    "vite": "^8.0.16"
  }
}
