{
  "name": "nuxt-app-example",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "pnpm exec turbo run build --filter=@pinagent/nuxt-plugin",
    "dev": "nuxt dev",
    "predev:dogfood": "pnpm exec turbo run build --filter=@pinagent/nuxt-plugin --filter=@pinagent/mcp",
    "dev:dogfood": "PINAGENT_SPAWN_AGENT=off nuxt dev",
    "build": "nuxt build",
    "preview": "nuxt preview"
  },
  "devDependencies": {
    "@pinagent/nuxt-plugin": "workspace:*",
    "nuxt": "^4.0.0",
    "typescript": "^6.0.3"
  }
}
