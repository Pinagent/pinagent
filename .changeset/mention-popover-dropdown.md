---
'@pinagent/next-plugin': patch
'@pinagent/vite-plugin': patch
---

Widget: the `@`-mention file picker in the comment composer now drops down from
the textarea's bottom edge instead of popping up above it, matching the
conventional direction for a typeahead menu.
