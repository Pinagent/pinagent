---
'@pinagent/vite-plugin': patch
'@pinagent/next-plugin': patch
---

Anchor-lost pins can now be opened (click to open the conversation in the dock) and dismissed (archive button removes the orphaned pin).
