# @pinagent/browser-runtime

In-browser runtime loaded into the host application: manages the SQLite-WASM cache, iframe lifecycle for the widget, and HMR re-anchoring of comments when source files change. Injected by `@pinagent/vite-plugin` and `@pinagent/next-plugin`.
