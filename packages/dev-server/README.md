# @pinagent/dev-server

Local agent runtime and WebSocket layer for Pinagent. Spawns Claude Agent SDK sessions per comment in isolated worktrees and streams progress back to the host page over WebSocket. Used by the OSS dev loop (`pinagent dev`).
