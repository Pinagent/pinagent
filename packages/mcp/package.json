{
  "name": "@pinpoint/mcp",
  "version": "0.0.1",
  "description": "stdio MCP server for Pinpoint — exposes pending feedback, screenshots, and source context to a coding agent.",
  "type": "module",
  "bin": {
    "pinpoint-mcp": "dist/index.js"
  },
  "main": "dist/index.js",
  "files": ["dist"],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "tsup": "^8.3.5",
    "typescript": "^5.6.3"
  }
}
