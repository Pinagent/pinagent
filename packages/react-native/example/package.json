{
  "name": "pinagent-expo-example",
  "version": "0.0.0",
  "private": true,
  "//": "Standalone Expo app — NOT a pnpm workspace member (the packages/* glob only matches direct children of packages/). Its Expo/RN deps never enter the monorepo lockfile. Run it on its own: `cd packages/react-native/example && npm install && npx expo start`.",
  "main": "node_modules/expo/AppEntry.js",
  "scripts": {
    "start": "expo start",
    "ios": "expo start --ios",
    "android": "expo start --android"
  },
  "dependencies": {
    "expo": "~51.0.0",
    "expo-status-bar": "~1.12.1",
    "react": "18.2.0",
    "react-native": "0.74.5",
    "react-native-svg": "15.2.0",
    "react-native-view-shot": "^3.8.0"
  },
  "devDependencies": {
    "@babel/core": "^7.24.0"
  }
}
